## Intializing Website locally

```bash
npm start
```

## Build the website (for production)

```bash
npm run build
```
or

```bash
npm run build -- --no-minify
```

> The build items will created at `/docs` folder in the root path

## Test the build locally (final website render)

```bash
npm run serve
```